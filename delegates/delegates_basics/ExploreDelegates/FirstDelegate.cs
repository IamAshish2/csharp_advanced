namespace ExploreDeletages
{
    // need to match the return type  of the method
    // need to match the parameter of the method
    public delegate void FirstDelegate(int number);
    public delegate void SaySomethingDelegate(string a);
}